rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Client access is denied. All data access occurs via the server-side API proxy.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
